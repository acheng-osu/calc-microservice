syntax = "proto3";

service Calculator {
    rpc CalculateString (calculateStringInput) returns (calculateStringOutput) {}
}

message calculateStringInput {
    string str = 1;
}

message calculateStringOutput {
    float result = 1;
}